---
description: Limites para uso de skills no crawler
alwaysApply: false
globs: ["src/scout_api/modules/crawler/**/*.py", "tests/fixtures/**/*"]
---

# Crawler

- Preserve `Fetch -> Store Adapter -> Offer / Details / Images -> Services` e a separação fetch/parsing.
- `scrapy-web-scraping` é referência **conceitual**: selectors, isolation de parsers, normalização, fixtures e validação.
- **Não** use a skill Scrapy para: migrar fetch, introduzir Splash/Playwright, Scrapy-Redis, fake-UA, middleware/scheduler paralelo, proxy rotation ou outro crawler.
- Não altere Camoufox, navegação, waits, fingerprint, proxies, retries, concorrência ou lifecycle sem pedido explícito; a exceção é cumprir Proxy Cost Mode.
- Preserve bloqueio versus indisponibilidade, resolução de variante e associação correta entre estoque e preço.
- Em validação live: soft-404 / “produto não encontrado” (ex. Magalu `Oops!`)
  **não** é, por si só, falha de integração — teste outro PDP válido da loja
  antes de abrir pendência de crawler (playbook da loja em `docs/crawler/stores/`).
- Imagens só quando solicitadas e nunca via proxy pago; consulte sempre `proxy-cost-mode.mdc`.
- Contratos e playbooks por loja: `docs/crawler/contracts.md` e `docs/crawler/stores/`. Atualize o playbook da loja quando mudar semântica duradoura.